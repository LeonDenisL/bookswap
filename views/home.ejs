<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>BookSwap - Home</title>
    <link rel="stylesheet" href="/css/home.css" />
    <link rel="stylesheet" href="/css/carousel.css" />
    <link
      rel="stylesheet"
      type="text/css"
      href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick.css"
    />
    <link
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick.min.js"></script>
    <script src="/js/carousel.js"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  </head>
  <body>
    <header class="bg-purple text-white py-3">
      <div class="container d-flex justify-content-between align-items-center">
        <h1 class="bookswap"><a href="/">BookSwap</a></h1>
        <nav>
          <ul class="nav">
            <% if (user) { %>
            <li class="nav-item">
              <a class="nav-link text-white" href="/profile">Perfil</a>
            </li>
            <% } %>

            <li class="nav-item">
              <a class="nav-link text-white" href="/publish-book"
                >Publicar Livro</a
              >
            </li>

            <% if (!user) { %>

            <li class="nav-item">
              <a class="nav-link text-white" href="/login">Login</a>
            </li>

            <li class="nav-item">
              <a class="nav-link text-white" href="/register">Registrar</a>
            </li>

            <% } %> <% if (user) { %>
            <li class="nav-item">
              <a class="nav-link text-white" href="/logout">Logout</a>
            </li>
            <% } %>

            <li class="nav-item">
              <a class="nav-link text-white" href="#sobre">Sobre</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-white" href="#contato">Contato</a>
            </li>
          </ul>
        </nav>
      </div>
    </header>
    <section class="intro bg-purple text-white text-center py-5" id="inicio">
      <div class="container2">
        <h2>Bem-vindo ao BookSwap</h2>
        <p>
          Troque livros facilmente com outros usuários. Cadastre seu livro e
          encontre novas histórias para ler.
        </p>
      </div>
    </section>

    <!-- Categoria de Novos Livros -->
    <section class="books py-5" id="trocar">
      <div class="container">
        <h2 class="text-center mb-5">Novos Livros Disponíveis</h2>
        <div class="books">
          <% if (newBooks.length > 0) { %> <% newBooks.forEach(function(book) {
          %>
          <div class="book">
            <a href="/book-details/<%= book.BookID %>">
              <!-- Adicionando link para detalhes do livro -->
              <img src="<%= book.CoverImageURL %>" alt="<%= book.Title %>" />
              <h3><%= book.Title %></h3>
              <p>Autor: <%= book.Author %></p>
              <p>Editora: <%= book.Publisher %></p>
            </a>
          </div>
          <% }); %> <% } else { %>
          <p>Nenhum livro novo disponível no momento.</p>
          <% } %>
        </div>
      </div>
    </section>

    <!-- Categoria Combina com Você -->
    <section class="recommended-books py-5">
      <div class="container">
        <h2 class="text-center mb-5">Combina com Você</h2>
        <div class="books">
          <% if (recommendedBooks.length > 0) { %> <%
          recommendedBooks.forEach(function(book) { %>
          <div class="book">
            <a href="/book-details/<%= book.BookID %>">
              <img src="<%= book.CoverImageURL %>" alt="<%= book.Title %>" />
              <h3><%= book.Title %></h3>
              <p>Autor: <%= book.Author %></p>
              <p>Editora: <%= book.Publisher %></p>
            </a>
          </div>
          <% }); %> <% } else { %>
          <p>Nenhum livro recomendado disponível no momento.</p>
          <% } %>
        </div>
      </div>
    </section>

    <!-- Carrossel de Livros -->
    <section class="carousel-section">
      <h2>Carrossel de Livros</h2>
      <div class="carousel">
        <% carouselBooks.forEach(book => { %>
        <div class="carousel-item">
          <div class="carousel-item-image">
            <img src="<%= book.CoverImageURL %>" alt="<%= book.Title %>" />
          </div>
          <div class="carousel-item-info">
            <h3><%= book.Title %></h3>
            <p>Autor: <%= book.Author %></p>
            <p>Sinopse: <%= book.Synopsis %></p>
          </div>
        </div>
        <% }); %>
      </div>
      <div>
        <!-- Adicione um carrossel com os últimos 5 livros -->
        <% if (carouselBooks.length > 0) { %>
        <!-- Exiba os livros do carrossel aqui -->
        <% } else { %>
        <p>Nenhum livro disponível para o carrossel.</p>
        <% } %>
      </div>
    </section>
    <footer class="bg-dark text-white text-center py-3">
      <div class="container">
        <p>&copy; 2024 BookSwap. Todos os direitos reservados.</p>
      </div>
    </footer>
  </body>
</html>
